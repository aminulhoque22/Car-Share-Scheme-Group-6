<div class="container">
<div class="row">
  <div class="col-md-12 ">
<% provide(:title, "Add new car") %>
<% if isAdmin? || isSuperAdmin? %>
  <h1 class="text-left">Add new car</h1>
<% else %>
  <h1  class="text-left">Suggest new car</h1>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', cars_path, :class => 'btn btn-link' %>
</div>
</div>
</div>